/* egg_tls.h — formerly the wolfSSL include shim.
 * wolfSSL has been replaced by opssl. This header is now empty and exists
 * only to avoid breaking any stale #include directives until they are
 * cleaned up.
 */

#ifndef EGG_TLS_H
#define EGG_TLS_H
#endif /* EGG_TLS_H */
